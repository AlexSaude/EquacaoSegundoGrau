// Projeto iniciado dia 10 de setembro de 2023
#include<stdlib.h>
#include<stdio.h>
#include<math.h>
#include<time.h>
#include<locale.h>

//x²= 0
// x² - x - 12 = 0    exemplo 1 equação completa
//3x² - 27 = 0        exemplo 2 equação incompleta por falta de b
//2x² - 4 = 0
//5x² - 45x = 0       exemplo 3 equação incompleta por falta de c
//-x² + 3x = 0
//b** - 4 ac;  delta

int main(){
system("color 13");
struct tm *ptr;
time_t lt;
lt = time(NULL);
ptr = localtime(&lt);
printf(asctime(ptr));


setlocale(LC_ALL,"Portuguese");
int a,b,c,delta,x1,x2;


printf("Digite o valor de a: "); scanf("%d", &a);
printf("Digite o valor de b: "); scanf("%d", &b);
printf("Digite o valor de c: "); scanf("%d", &c);

if(a == 0){
    printf("Equação precisa ter primeira incógta.");
    }
    else if(b == 0){
        printf("Equação incompleta por falta do segundo elemento.\n");
    }
    else if(c == 0){
        printf("Equação incompleta por falta do terceiro elemento.\n");
    }

delta = b*b - (4*a*c);
x1 = (-b + sqrt(delta))/ (2*a);
x2 = (-b - sqrt(delta)) / (2*a);

printf("delta = %d", delta);
printf("\n x1 e %d", x1);
printf("\n x2 e %d", x2);


return 0;
}
